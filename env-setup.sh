#!/bin/bash

#This is for installing dotnet-format
dotnet tool install -g dotnet-format

#This is for installing ReSharper.GlobalTools. It might take couple minutes to install.
dotnet tool install -g JetBrains.ReSharper.GlobalTools